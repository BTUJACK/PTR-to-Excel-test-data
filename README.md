# PTR-to-Excel-test-data
把PTR放到Excel表格，第二列放测试结果，第三列放链接


针对Excel文档使用Python 3.8.7 版本执行自动化操作，并且用Python生成一个用户界面：
1
如果第一列某一行含有"："并且"："后面没有其他字符，加粗第一列；合并前三列；禁止合并多行；

2
如果第一列某一行第一个字符是数字第二个字符不是括号，加粗第一列；合并前三列，合并后的内容靠左对齐；禁止合并多行；
3
文档特点：每页的第一行相同的，都是模板，不用修改
